# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control

# ============================================
# REQUIRED: WordPress GraphQL Configuration
# ============================================
# Your WordPress site's GraphQL endpoint
# Usually: https://your-domain.com/graphql (with WPGraphQL plugin)
WORDPRESS_API_URL=https://example.com/graphql

# ============================================
# OPTIONAL: WordPress Authentication
# ============================================
# Bearer token for authenticated GraphQL requests
# Only needed if your GraphQL endpoint requires authentication
# See SETUP.md for how to generate this token
WORDPRESS_AUTH_REFRESH_TOKEN=your_refresh_token_here

# ============================================
# REQUIRED: Site Configuration
# ============================================
# Base URL of your website (without trailing slash)
# Used in sitemaps, metadata, and sharing
NEXT_PUBLIC_BASE_URL=https://blog.example.com

# ============================================
# OPTIONAL: CDN Configuration
# ============================================
# CloudFront distribution domain for optimized image serving
# Example: d1234567890abc.cloudfront.net
NEXT_PUBLIC_CLOUDFRONT_DOMAIN=d1234567890abc.cloudfront.net

# ============================================
# OPTIONAL: Revalidation & Caching
# ============================================
# Secret key for ISR (Incremental Static Regeneration) revalidation
# Used to securely revalidate cached pages
# Generate a strong random string for production
REVALIDATION_SECRET=your_secret_key_here
